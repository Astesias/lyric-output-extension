# ── 敏感配置（含 Cookie / csrf_token，切勿上传）──
config.json

# ── 运行时缓存与临时文件 ──
lyric_cache.json
_tmp_*
*.log

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ── Node ──
node_modules/
*.vsix

# ── 编辑器 / 系统 ──
.DS_Store
Thumbs.db
.vscode/settings.json
